# Connect2Roots Admin Backend API (for contact, partnership, mentor application, donations)
# Leave empty to run without backend (forms will only log to console).
VITE_API_URL=http://localhost:4000

# Mentor module platform (landing + auth). Leave empty to use in-app Login and no platform redirect.
# Example: http://localhost:3000 (when mentor_module runs on 3000) or https://mentor.connect2roots.org
VITE_MENTOR_PLATFORM_URL=http://localhost:3001
